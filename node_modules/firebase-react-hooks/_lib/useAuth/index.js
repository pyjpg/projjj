export function firebaseOnAuthStateChanged(auth, setUser) {
    auth.onAuthStateChanged(userData => {
        if (userData) {
            setUser(userData);
        }
        else {
            setUser(null);
        }
    });
}
//# sourceMappingURL=index.js.map