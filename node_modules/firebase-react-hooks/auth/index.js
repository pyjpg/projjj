import firebase from 'firebase/app';
import 'firebase/auth';
import { firebaseSendSignInLink, firebaseSignIn, firebaseSignInWithLink, firebaseSignOut } from '../_lib/auth';
export function signIn(provider, options) {
    return firebaseSignIn(firebase.auth(), firebase.auth, provider, options);
}
export function sendSignInLink(options) {
    return firebaseSendSignInLink(firebase.auth(), options);
}
export function signInWithLink(options) {
    return firebaseSignInWithLink(firebase.auth(), options);
}
export function signOut() {
    return firebaseSignOut(firebase.auth());
}
//# sourceMappingURL=index.js.map